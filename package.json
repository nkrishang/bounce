{
  "name": "bounce-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "docker compose up -d redis && pnpm run --parallel dev",
    "dev:fresh": "docker compose up -d redis && docker compose exec redis redis-cli FLUSHALL && pnpm run --parallel dev",
    "dev:no-redis": "pnpm run --parallel dev",
    "cache:clear": "docker compose exec redis redis-cli FLUSHALL",
    "build": "pnpm run -r build",
    "lint": "pnpm run -r lint",
    "typecheck": "pnpm run -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=20.9.0"
  }
}
